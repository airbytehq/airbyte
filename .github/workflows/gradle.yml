name: Dataline CI

on: push

jobs:
  build:
    env:
      GRADLE_PATH: /tmp/gradle
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Dataline
        uses: actions/checkout@v2

      - name: Cache java deps
        uses: actions/cache@v2
        with:
          path: ${{ env.GRADLE_PATH }}
          key: gradle-${{ hashFiles('**/*.gradle') }}
          restore-keys: |
            gradle-

      - uses: actions/setup-java@v1
        with:
          java-version: '14'

      - name: Build
        run: ./gradlew build -g ${{ env.GRADLE_PATH }}

      - name: Docker
        run: find /var/lib/docker
#      - name: Build
#        run: ./tools/app/build.sh
#      - name: Test
#        run: ./tools/app/test.sh

