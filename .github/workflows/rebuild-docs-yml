name: Rebuild Docs

on:
  schedule:
    - cron: "0 0 * * *"

jobs:
  rebuild-docs:
    runs-on: ubuntu-latest
    steps:
      - name: Call Deploy Hook
        run: curl -X POST ${{ secrets.VERCEL_DOCS_DEPLOY_HOOK_URL }}
