name: PR unpin connections on merge
on:
  pull_request:
    types: [closed]

jobs:
  ai-unpin:
    runs-on: ubuntu-latest
    name: AI Unpin Connections
    steps:
      - name: No-op
        run: true
