codecov:
  notify:
    require_ci_to_pass: no

coverage:
  status:
    patch:
      default:
        target: 90%
        if_no_uploads: error
        if_not_found: failure
        if_ci_failed: failure
    project:
      default:
        target: 90%
        if_no_uploads: error
        if_not_found: failure
        if_ci_failed: failure
