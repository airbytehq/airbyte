plugins {
    id 'application'
}

task prepareBuild(type: Copy) {
    from layout.projectDirectory.file("nginx-auth.conf"), layout.projectDirectory.file("nginx-no-auth.conf"), layout.projectDirectory.file("run.sh")
    into layout.buildDirectory.dir("docker")
}

tasks.named("buildDockerImage") {
    dependsOn prepareBuild
    dependsOn copyDocker
}
