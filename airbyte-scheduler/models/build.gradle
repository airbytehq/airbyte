plugins {
    id "java-library"
}

dependencies {
    implementation project(':airbyte-config:models')
    implementation project(':airbyte-protocol:models')
}

Task publishArtifactsTask = getPublishArtifactsTask("airbyte-scheduler-models", "$rootProject.ext.version", project)
