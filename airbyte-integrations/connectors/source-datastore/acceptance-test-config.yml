# See https://docs.airbyte.com/connector-development/testing-connectors/connector-acceptance-tests-reference
# for more information about how to configure these tests
connector_image: airbyte/source-datastore:dev
test_strictness_level: low
acceptance_tests:
  spec:
    tests:
      - spec_path: source_datastore/spec.yaml
  # The following tests require a real GCP service account in secrets/config.json.
  # They are disabled here because credentials are not available in unprivileged CI runs.
  # To run them locally: place a valid config in secrets/config.json and uncomment.
  # connection:
  #   tests:
  #     - config_path: secrets/config.json
  #       status: succeed
  #     - config_path: integration_tests/invalid_config.json
  #       status: failed
  # discovery:
  #   tests:
  #     - config_path: secrets/config.json
  # basic_read:
  #   tests:
  #     - config_path: secrets/config.json
  #       timeout_seconds: 120
