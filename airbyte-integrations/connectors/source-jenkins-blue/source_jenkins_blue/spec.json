{
  "documentationUrl": "https://docs.airbyte.io/integrations/sources/jenkins-blue",
  "connectionSpecification": {
    "$schema": "http://json-schema.org/draft-07/schema#",
    "title": "Jenkins Blue Spec",
    "type": "object",
    "required": ["base_url", "username", "api_token"],
    "additionalProperties": false,
    "properties": {
      "base_url": {
        "type": "string",
        "title": "Base URL",
        "description": "The base url for your jenkins instance, including everything before the '/blue/rest' portion.",
        "examples": ["https://example.com/jenkins", "https://jenkins.example.com"]
      },
      "username": {
        "type": "string",
        "title": "Username",
        "description": "The user account on the Jenkins server."
      },
      "api_token": {
        "type": "string",
        "title": "API Token",
        "description": "The Jenkins API token associated with the username.",
        "airbyte_secret": true
      },
      "start_date": {
        "type": "string",
        "title": "Start Date",
        "description": "The date from which you'd like to replicate data from Jenkins in the format YYYY-MM-DDT00:00:00Z.",
        "examples": ["2022-01-01T00:00:00Z"],
        "pattern": "^[0-9]{4}-[0-9]{2}-[0-9]{2}T[0-9]{2}:[0-9]{2}:[0-9]{2}(.[0-9]*)?Z$"
      },
      "pipelines": {
        "type": "string",
        "title": "Pipelines",
        "description": "Space separated list of full pipeline names with unix shell style pattern matching.",
        "examples": ["pipeline1", "folder/folder/*", "prefix-*"]
      }
    }
  }
}
