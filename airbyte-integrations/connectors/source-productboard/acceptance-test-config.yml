# See [Connector Acceptance Tests](https://docs.airbyte.com/connector-development/testing-connectors/connector-acceptance-tests-reference)
# for more information about how to configure these tests
connector_image: airbyte/source-productboard:dev
acceptance_tests:
  spec:
    tests:
      - spec_path: "manifest.yaml"
  connection:
    bypass_reason: "Community contribution; no CI secret available. V2 migration validated locally via the Connector Builder against the Productboard V2 API."
  discovery:
    bypass_reason: "Community contribution; no CI secret available. V2 migration validated locally via the Connector Builder against the Productboard V2 API."
  basic_read:
    bypass_reason: "Community contribution; no CI secret available. V2 migration validated locally via the Connector Builder against the Productboard V2 API."
  incremental:
    bypass_reason: "Community contribution; no CI secret available. V2 migration validated locally via the Connector Builder against the Productboard V2 API."
  full_refresh:
    bypass_reason: "Community contribution; no CI secret available. V2 migration validated locally via the Connector Builder against the Productboard V2 API."
