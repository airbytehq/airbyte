[build-system]
requires = ["poetry-core=^1.0.0"]
build-backend = "poetry.core.masonry.api"
[tool.poetry]
name = "source-amazon-seller-partner-tests"
version = "0.0.0"
description = "Unit tests for source-amazon-seller-partner"
authors = ["Airbyte <contact@airbyte.io>"]
package-mode = false
[tool.poetry.dependencies]
python = "^3.10,<3.13"
# DO NOT MERGE as-is: prerelease built from airbytehq/airbyte-python-cdk#1149, which ships the
# REDUCE_PAGE_SIZE action. Move to the released CDK once that PR is merged.
airbyte-cdk = "7.28.3.post14.dev35239229954"
pytest = "^8"
pytest-lazy-fixtures = "^1.1.4"
requests-mock = "^1.12.1"
freezegun = "^1.5.2"
pendulum = "^3.1.0"
dateparser = "^1.2.1"
pytest-mock = "^3.14.1"
[tool.pytest.ini_options]
filterwarnings = [
    "ignore:This class is experimental*"
]
