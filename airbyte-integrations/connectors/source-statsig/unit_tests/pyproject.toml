[build-system]
requires = [ "poetry-core>=1.0.0",]
build-backend = "poetry.core.masonry.api"

[tool.poetry]
name = "source-statsig-tests"
version = "0.0.50"
description = "Unit tests for source-statsig"
authors = ["Airbyte <contact@airbyte.io>"]

[tool.poetry.dependencies]
python = "^3.10,<3.13"
pytest = "^8"
PyYAML = "^6"
