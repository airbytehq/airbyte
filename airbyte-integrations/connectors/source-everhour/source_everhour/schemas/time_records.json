{
  "$schema": "http://json-schema.org/draft-04/schema#",
  "type": "object",
  "additionalProperties": true,
  "properties": {
    "id": {
      "description": "The unique identifier of the time record entry",
      "type": ["null", "integer"]
    },
    "date": {
      "description": "The date of the time record entry",
      "type": ["null", "string"]
    },
    "createdAt": {
      "description": "The timestamp when the time record was created",
      "type": ["null", "string"]
    },
    "user": {
      "description": "The user associated with the time record entry",
      "type": ["null", "integer"]
    },
    "time": {
      "description": "Time spent on the task record",
      "type": ["null", "integer"]
    },
    "task": {
      "type": ["null", "object"],
      "properties": {
        "id": {
          "description": "The unique identifier of the task",
          "type": ["null", "string"]
        },
        "name": {
          "description": "The name or title of the task",
          "type": ["null", "string"]
        },
        "type": {
          "description": "The type or category of the task",
          "type": ["null", "string"]
        },
        "status": {
          "description": "The status of the task",
          "type": ["null", "string"]
        },
        "url": {
          "description": "URL link to access the task details",
          "type": ["null", "string"]
        },
        "iteration": {
          "description": "Iteration number associated with the task",
          "type": ["null", "string"]
        },
        "projects": {
          "type": ["null", "array"],
          "items": {
            "description": "List of projects the task is associated with"
          }
        },
        "createdAt": {
          "description": "The timestamp when the task was created",
          "type": ["null", "string"]
        },
        "labels": {
          "type": ["null", "array"],
          "items": {
            "description": "List of labels associated with the task"
          }
        },
        "time": {
          "type": ["null", "object"],
          "properties": {
            "total": {
              "description": "Total time spent on the task",
              "type": ["null", "integer"]
            },
            "users": {
              "type": ["null", "object"],
              "properties": {
                "1029023": {
                  "description": "Time spent on the task by User ID 1029023",
                  "type": ["null", "integer"]
                },
                "1163985": {
                  "description": "Time spent on the task by User ID 1163985",
                  "type": ["null", "integer"]
                }
              }
            },
            "timerTime": {
              "description": "Time spent using a timer for the task",
              "type": ["null", "integer"]
            }
          }
        },
        "completed": {
          "description": "Flag indicating whether the task is completed",
          "type": ["null", "boolean"]
        },
        "assignees": {
          "type": ["null", "array"],
          "items": {
            "description": "List of assignees for the task"
          }
        }
      }
    }
  }
}
