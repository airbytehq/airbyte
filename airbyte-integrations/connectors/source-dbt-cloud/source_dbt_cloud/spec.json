{
  "documentationUrl": "https://docsurl.com",
  "connectionSpecification": {
    "$schema": "http://json-schema.org/draft-07/schema#",
    "title": "DBT Cloud Spec",
    "type": "object",
    "required": ["account_id", "authorization"],
    "additionalProperties": false,
    "properties": {
      "account_id": {
        "type": "integer",
        "description": "The numeric ID of the account containing jobs.",
        "airbyte_secret": true
      },
      "authorization": {
        "type": "string",
        "description": "The DBT Cloud API key (which, as a reminder, uses a token auth).",
        "airbyte_secret": true
      },
      "base_url": {
        "type": "string",
        "description": "The DBT Cloud API key (which, as a reminder, uses a token auth).",
        "airbyte_secret": false
      }
    }
  }
}
