{
  "type": "object",
  "properties": {
    "name": {
      "description": "Name of the segment that helps identify its purpose.",
      "type": ["string", "null"],
      "examples": ["Segment of Automation 1"]
    },
    "logic": {
      "description": "Logical criteria applied to the segment for filtering contacts.",
      "type": ["string", "null"],
      "examples": ["and"]
    },
    "hidden": {
      "description": "Indicates whether the segment is hidden or not.",
      "type": ["string", "null"],
      "examples": ["0"]
    },
    "seriesid": {
      "description": "Identifier for the series or sequence the segment belongs to.",
      "type": ["string", "null"],
      "examples": ["1"]
    },
    "links": {
      "description": "Related links or URLs associated with the segment.",
      "type": ["array", "null"]
    },
    "id": {
      "description": "Unique identifier for the segment.",
      "type": "string",
      "examples": ["1"]
    }
  },
  "required": ["id"]
}
