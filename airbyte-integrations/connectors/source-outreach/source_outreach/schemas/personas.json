{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "additionalProperties": true,
  "properties": {
    "id": {
      "description": "The unique identifier for the persona",
      "type": "integer"
    },
    "name": {
      "description": "The name of the persona",
      "type": ["null", "string"]
    },
    "description": {
      "description": "The detailed description of the persona",
      "type": ["null", "string"]
    },
    "createdAt": {
      "description": "The timestamp when the persona data was created",
      "type": ["null", "string"],
      "format": "date-time"
    },
    "updatedAt": {
      "description": "The timestamp when the persona data was last updated",
      "type": ["null", "string"],
      "format": "date-time"
    },
    "prospects": {
      "description": "An array containing the prospects associated with the persona",
      "type": ["null", "array"],
      "items": {
        "type": ["null", "integer"]
      }
    }
  }
}
