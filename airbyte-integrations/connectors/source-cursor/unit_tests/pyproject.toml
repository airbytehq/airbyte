[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"

[tool.poetry]
name = "source-cursor-tests"
version = "0.0.1"
description = "Unit tests for source-cursor"
authors = ["Airbyte <contact@airbyte.io>"]
package-mode = false

[tool.poetry.dependencies]
python = "^3.10,<3.13"
airbyte-cdk = "7.23.7"
freezegun = "^1.5"
pytest = "^8"
pyyaml = "^6"
requests-mock = "^1"
