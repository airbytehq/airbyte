[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"

[tool.poetry]
name = "source-zoho-books-tests"
version = "0.0.0"
description = "Unit tests for source-zoho-books"
authors = ["Airbyte <contact@airbyte.io>"]

[tool.poetry.dependencies]
python = "^3.10,<3.13"
pytest = "^8"
PyYAML = "^6.0.3"
