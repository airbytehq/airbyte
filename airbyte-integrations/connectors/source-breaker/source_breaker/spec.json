{
  "documentationUrl": "https://docs.airbyte.com/integrations/sources/breaker",
  "connectionSpecification": {
    "$schema": "http://json-schema.org/draft-07/schema#",
    "title": "Breaker Source Spec",
    "type": "object",
    "required": [],
    "additionalProperties": true,
    "properties": {
      "check_outcome": {
        "title": "Outcome of check method",
        "description": "foobar",
        "type": "string",
        "order": 5,
        "default": "Check should succeed",
        "enum": ["Check should succeed", "Checking the connection should fail (user error)", "The check job should fail (unexpected connector error)"]
      },
      "discover_should_fail": {
        "title": "Fail Discover",
        "description": "Check this if you want catalog discovery to fail.",
        "type": "boolean",
        "default": false,
        "order": 6
      }
    }
  }
}
