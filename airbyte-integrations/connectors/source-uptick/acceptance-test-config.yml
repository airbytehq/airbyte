# See [Connector Acceptance Tests](https://docs.airbyte.com/connector-development/testing-connectors/connector-acceptance-tests-reference)
# for more information about how to configure these tests
connector_image: airbyte/source-uptick:dev
acceptance_tests:
  spec:
    tests:
      - spec_path: "manifest.yaml"
  connection:
    tests:
      - config_path: "secrets/config.json"
        status: "succeed"
  discovery:
    tests:
      - config_path: "secrets/config.json"
  basic_read:
    tests:
      - config_path: "secrets/config.json"
        configured_catalog_path: "integration_tests/configured_catalog.json"
        empty_streams:
          - name: creditnotelineitems
            bypass_reason: "Endpoint only allows GET in the sandbox tenant (observed via OPTIONS/POST probing); credit-note line items cannot be created in the sandbox tenant"
          - name: defectquotelineitems
            bypass_reason: "Not seedable in the certification sandbox tenant: the endpoint rejects writes (Uptick deprecated DefectQuoteLineItem CRUD in API v2.16; the connector pins v2.15)"
          - name: remarkevents
            bypass_reason: "Endpoint only allows GET in the sandbox tenant (observed via OPTIONS/POST probing); creating/updating remarks in the sandbox tenant does not emit remark events"
          - name: majorservices
            bypass_reason: "Endpoint only allows GET in the sandbox tenant (observed via OPTIONS/POST probing); no write path exists to seed major services in the sandbox tenant"
          - name: promptquestions
            bypass_reason: "Endpoint only allows GET in the sandbox tenant (observed via OPTIONS/POST probing); prompt questions cannot be created in the sandbox tenant"
          - name: promptanswers
            bypass_reason: "Endpoint only allows GET in the sandbox tenant (observed via OPTIONS/POST probing); prompt answers cannot be created in the sandbox tenant"
          - name: servicequotefixedlineitems
            bypass_reason: "POST returns 201 in the sandbox tenant but records never appear in the collection endpoint, so the stream cannot be seeded"
          - name: servicequotedoandchargelineitems
            bypass_reason: "POST returns 201 in the sandbox tenant but records never appear in the collection endpoint, so the stream cannot be seeded"
  incremental:
    tests:
      - config_path: secrets/config.json
        configured_catalog_path: integration_tests/configured_catalog.json
        future_state:
          future_state_path: integration_tests/abnormal_state.json
  full_refresh:
    tests:
      - config_path: "secrets/config.json"
        configured_catalog_path: "integration_tests/configured_catalog.json"
