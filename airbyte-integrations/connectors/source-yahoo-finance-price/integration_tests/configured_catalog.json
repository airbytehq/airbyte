{
  "streams": [
    {
      "stream": {
        "name": "price",
        "json_schema": {
          "type": "object",
          "properties": {
            "ticker": {
              "type": "string"
            }
          }
        },
        "supported_sync_modes": ["full_refresh"]
      },
      "sync_mode": "full_refresh",
      "destination_sync_mode": "overwrite"
    }
  ]
}
