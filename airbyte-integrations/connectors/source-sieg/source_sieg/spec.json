{
  "documentationUrl": "https://docsurl.com",
  "connectionSpecification": {
    "$schema": "http://json-schema.org/draft-07/schema#",
    "title": "Sieg Spec",
    "type": "object",
    "required": ["email","api_key", "start_date", "end_date"],
    "additionalProperties": false,
    "properties": {
      "email": {
        "title": "Email",
        "type": "string",
        "description": "Email used to retrieve data from the Sieg API."
      },
      "api_key": {
        "title": "API key",
        "type": "string",
        "description": "API key used to retrieve data from the Sieg API."
      },
      "start_date": {
        "title": "start_date",
        "type": "string",
        "description": "Start date in the format YYYY-MM-DD"
      },
      "end_date": {
        "title": "end_date",
        "type": "string",
        "description": "End date in the format YYYY-MM-DD"
      }
    }
  }
}
