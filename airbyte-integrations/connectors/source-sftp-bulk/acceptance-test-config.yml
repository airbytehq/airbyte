# See [Connector Acceptance Tests](https://docs.airbyte.io/connector-development/testing-connectors/connector-acceptance-tests-reference)
# for more information about how to configure these tests
connector_image: airbyte/source-sftp-bulk:dev
acceptance_tests:
  spec:
    tests:
      - spec_path: "source_sftp_bulk/spec.json"
        timeout_seconds: 60
        backward_compatibility_tests_config:
          disable_for_version: 0.3.2 # `start_date` format changed to format: date-time
  connection:
    bypass_reason: "This connector uses integration tests"
  #    tests:
  #      - config_path: "integration_tests/configs/config_password.json"
  #        status: "succeed"
  #        timeout_seconds: 60
  #      - config_path: "integration_tests/invalid_config.json"
  #        status: "failed"
  #        timeout_seconds: 60
  discovery:
    bypass_reason: "This connector uses integration tests"
  #    tests:
  #      - config_path: "integration_tests/configs/config_password.json"
  basic_read:
    bypass_reason: "This connector uses integration tests"
  #    tests:
  #      - config_path: "integration_tests/configs/config_password.json"
  #        configured_catalog_path: "integration_tests/configured_catalog.json"
  #        empty_streams: [ ]
  full_refresh:
    bypass_reason: "This connector uses integration tests"
  #    tests:
  #      - config_path: "integration_tests/configs/config_password.json"
  #        configured_catalog_path: "integration_tests/configured_catalog.json"
  incremental:
    bypass_reason: "This connector uses integration tests"
#    tests:
#      - config_path: "integration_tests/configs/config_password.json"
#        configured_catalog_path: "integration_tests/configs/configured_catalog.json"
#        future_state:
#          future_state_path: "integration_tests/abnormal_state.json"