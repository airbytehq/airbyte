version: 5.17.0

type: DeclarativeSource

check:
  type: CheckStream
  stream_names:
    - channel_messages

definitions:
  streams:
    # Parent stream: channel messages (incremental)
    # Fetches messages from a single configured channel
    channel_messages:
      type: DeclarativeStream
      name: channel_messages
      primary_key:
        - channel_id
        - ts
      retriever:
        type: SimpleRetriever
        requester:
          $ref: "#/definitions/base_requester"
          path: conversations.history
          request_parameters:
            limit: "1000"
            channel: "{{ config['channel_id'] }}"
        record_selector:
          type: RecordSelector
          extractor:
            type: DpathExtractor
            field_path:
              - messages
          record_filter:
            type: RecordFilter
            condition: "{{ record.get('type') == 'message' and not record.get('subtype') }}"
        paginator:
          type: DefaultPaginator
          page_token_option:
            type: RequestOption
            inject_into: request_parameter
            field_name: cursor
          pagination_strategy:
            type: CursorPagination
            cursor_value: "{{ response.get('response_metadata', {}).get('next_cursor', '') }}"
            stop_condition: "{{ not response.get('response_metadata', {}).get('next_cursor', '') }}"
      incremental_sync:
        type: DatetimeBasedCursor
        cursor_field: float_ts
        cursor_datetime_formats:
          - "%s.%f"
        datetime_format: "%s.%f"
        start_datetime:
          type: MinMaxDatetime
          datetime: "{{ config['start_date'] }}"
          datetime_format: "%Y-%m-%dT%H:%M:%SZ"
        start_time_option:
          type: RequestOption
          field_name: oldest
          inject_into: request_parameter
      transformations:
        - type: AddFields
          fields:
            - path:
                - float_ts
              value: "{{ record.ts|float }}"
            - path:
                - channel_id
              value: "{{ config['channel_id'] }}"
      schema_loader:
        type: InlineSchemaLoader
        schema:
          $ref: "#/schemas/channel_messages"

    # Child stream: threads (with incremental_dependency)
    threads:
      type: DeclarativeStream
      name: threads
      primary_key:
        - channel_id
        - ts
      retriever:
        type: SimpleRetriever
        requester:
          $ref: "#/definitions/base_requester"
          path: conversations.replies
          request_parameters:
            limit: "1000"
            channel: "{{ config['channel_id'] }}"
        record_selector:
          type: RecordSelector
          extractor:
            type: DpathExtractor
            field_path:
              - messages
        paginator:
          type: DefaultPaginator
          page_token_option:
            type: RequestOption
            inject_into: request_parameter
            field_name: cursor
          pagination_strategy:
            type: CursorPagination
            cursor_value: "{{ response.get('response_metadata', {}).get('next_cursor', '') }}"
            stop_condition: "{{ not response.get('response_metadata', {}).get('next_cursor', '') }}"
        partition_router:
          type: SubstreamPartitionRouter
          parent_stream_configs:
            - type: ParentStreamConfig
              stream:
                $ref: "#/definitions/streams/channel_messages"
              # This is the KEY setting being tested:
              # When true, should only iterate over NEW parent records on incremental sync
              # Works on OSS, broken on Cloud
              incremental_dependency: true
              parent_key: ts
              partition_field: float_ts
              request_option:
                type: RequestOption
                field_name: ts
                inject_into: request_parameter
      incremental_sync:
        type: DatetimeBasedCursor
        cursor_field: float_ts
        cursor_datetime_formats:
          - "%s.%f"
        datetime_format: "%s.%f"
        start_datetime:
          type: MinMaxDatetime
          datetime: "{{ config['start_date'] }}"
          datetime_format: "%Y-%m-%dT%H:%M:%SZ"
      transformations:
        - type: AddFields
          fields:
            - path:
                - float_ts
              value: "{{ record.ts|float }}"
            - path:
                - channel_id
              value: "{{ config['channel_id'] }}"
            - path:
                - parent_message_ts
              value: "{{ stream_partition['float_ts'] }}"
      schema_loader:
        type: InlineSchemaLoader
        schema:
          $ref: "#/schemas/threads"

  base_requester:
    type: HttpRequester
    url_base: https://slack.com/api/
    authenticator:
      type: BearerAuthenticator
      api_token: "{{ config['api_token'] }}"
    error_handler:
      type: DefaultErrorHandler
      response_filters:
        - error_message_contains: "invalid_auth"
          action: FAIL
          error_message: "Authentication failed. Please check your API token."
        - http_codes: [429]
          action: RETRY
          backoff_strategies:
            - type: WaitTimeFromHeader
              header: Retry-After
        - http_codes: [500, 503]
          action: RETRY

streams:
  - $ref: "#/definitions/streams/channel_messages"
  - $ref: "#/definitions/streams/threads"

spec:
  type: Spec
  connection_specification:
    type: object
    $schema: http://json-schema.org/draft-07/schema#
    required:
      - api_token
      - channel_id
      - start_date
    properties:
      api_token:
        type: string
        title: API Token
        description: Slack Bot Token (starts with xoxb-) with scopes channels:read and channels:history
        airbyte_secret: true
        order: 0
      channel_id:
        type: string
        title: Channel ID
        description: Slack channel ID to test with (e.g. C0AG3UM1RQC). Find it in Slack by right-clicking the channel → View channel details → Copy Channel ID
        examples:
          - "C0AG3UM1RQC"
        order: 1
      start_date:
        type: string
        title: Start Date
        description: Start date for incremental sync (use 2020-01-01 to capture all messages)
        format: date-time
        pattern: "^[0-9]{4}-[0-9]{2}-[0-9]{2}T[0-9]{2}:[0-9]{2}:[0-9]{2}Z$"
        examples:
          - "2020-01-01T00:00:00Z"
        order: 2
    additionalProperties: true

metadata:
  autoImportSchema:
    channel_messages: false
    threads: false

schemas:
  channel_messages:
    type: object
    $schema: http://json-schema.org/draft-07/schema#
    additionalProperties: true
    properties:
      channel_id:
        type: ["null", "string"]
        description: Channel ID
      ts:
        type: ["null", "string"]
        description: Message timestamp
      float_ts:
        type: ["null", "number"]
        description: Message timestamp as float
      text:
        type: ["null", "string"]
        description: Message text
      user:
        type: ["null", "string"]
        description: User ID who posted the message

  threads:
    type: object
    $schema: http://json-schema.org/draft-07/schema#
    additionalProperties: true
    properties:
      channel_id:
        type: ["null", "string"]
        description: Channel ID
      ts:
        type: ["null", "string"]
        description: Reply timestamp
      float_ts:
        type: ["null", "number"]
        description: Reply timestamp as float
      parent_message_ts:
        type: ["null", "number"]
        description: Parent message timestamp (which message this is a reply to)
      text:
        type: ["null", "string"]
        description: Reply text
      user:
        type: ["null", "string"]
        description: User ID who posted the reply
