{
  "streams": [
    {
      "stream": {
        "name": "sample_data",
        "json_schema": {
          "$schema": "http://json-schema.org/draft-07/schema#",
          "type": "object",
          "properties": {
            "Game Number": { "type": ["number", "null"] },
            " \"Game Length\"": { "type": ["number", "null"] }
          },
          "supported_sync_modes": ["full_refresh"]
        },
        "supported_sync_modes": ["full_refresh"]
      },
      "sync_mode": "full_refresh",
      "destination_sync_mode": "overwrite"
    }
  ]
}
