{
  "documentationUrl": "https://docs.airbyte.io/integrations/sources/file-source",
  "connectionSpecification": {
    "$schema": "http://json-schema.org/draft-07/schema#",
    "title": "File source Spec",
    "type": "object",
    "required": ["data_url"],
    "additionalProperties": true,
    "properties": {
      "data_url": {
        "type": "string",
        "description": "The path to the file to be replicated."
      },
      "reader": {
        "type": "string",
        "description": "Which reader to use for parsing the file format (csv, json, parquet, etc)"
      },
      "reader_arguments": {
        "type": "object",
        "description": "Extra arguments to provide to the reader"
      }
    }
  }
}
