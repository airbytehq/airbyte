{
  "$schema": "http://json-schema.org/draft-04/schema#",
  "type": "object",
  "additionalProperties": true,
  "properties": {
    "followerCountsByStaffCountRange": {
      "description": "Follower counts grouped by staff count range.",
      "type": ["null", "array"],
      "items": {
        "type": ["null", "object"],
        "additionalProperties": true
      }
    },
    "followerCountsByFunction": {
      "description": "Follower counts grouped by function.",
      "type": ["null", "array"],
      "items": {
        "type": ["null", "object"],
        "additionalProperties": true
      }
    },
    "followerCountsByAssociationType": {
      "description": "Follower counts grouped by association type.",
      "type": ["null", "array"],
      "items": {
        "type": ["null", "object"],
        "additionalProperties": true
      }
    },
    "followerCountsBySeniority": {
      "description": "Follower counts grouped by seniority level.",
      "type": ["null", "array"],
      "items": {
        "type": ["null", "object"],
        "additionalProperties": true
      }
    },
    "followerCountsByRegion": {
      "description": "Follower counts grouped by region/area.",
      "type": ["null", "array"],
      "items": {
        "type": ["null", "object"],
        "additionalProperties": true
      }
    },
    "organizationalEntity": {
      "description": "Information about the organizational entity these statistics are related to.",
      "type": ["null", "string"]
    },
    "followerCountsByCountry": {
      "description": "Follower counts grouped by country.",
      "type": ["null", "array"],
      "items": {
        "type": ["null", "object"],
        "additionalProperties": true
      }
    },
    "followerCountsByIndustry": {
      "description": "Follower counts grouped by industry.",
      "type": ["null", "array"],
      "items": {
        "type": ["null", "object"],
        "additionalProperties": true
      }
    }
  }
}
