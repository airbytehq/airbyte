# destination-elasticsearch-strict-encrypt: Contributor notes

## Test configuration

To test this destination, run Elasticsearch with `xpack.security.enabled`.

This connector inherits the Elasticsearch destination and only supports authorized connections.
