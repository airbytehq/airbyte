# See [Connector Acceptance Tests](https://docs.airbyte.com/connector-development/testing-connectors/connector-acceptance-tests-reference)
# for more information about how to configure these tests
connector_image: airbyte/source-greenhouse:dev
test_strictness_level: "high"
acceptance_tests:
  spec:
    tests:
      - spec_path: "manifest.yaml"
  connection:
    tests:
      - config_path: "secrets/config_oauth.json"
        status: "succeed"
      - config_path: "integration_tests/config_invalid.json"
        status: "failed"
  discovery:
    tests:
      - config_path: "secrets/config_oauth.json"
  basic_read:
    tests:
      # Restore expect_records from a regenerated live Harvest v3 read before enabling fixture validation.
      - config_path: "secrets/config_oauth.json"
        empty_streams:
          # Every bypassed stream has a mock read test in unit_tests/test_pagination.py.
          - name: eeoc
            bypass_reason: "Test account has no submitted EEOC responses"
          - name: prospect_pools
            bypass_reason: "Test account has no prospect pools"
          - name: tags
            bypass_reason: "Test account has no candidate tags"
          - name: user_permissions
            bypass_reason: "Test account has no job-level user permissions; the only users are site admins, who never appear in this resource"
  full_refresh:
    tests:
      - config_path: "secrets/config_oauth.json"
        configured_catalog_path: "integration_tests/configured_catalog.json"
  incremental:
    tests:
      - config_path: "secrets/config_oauth.json"
        configured_catalog_path: "integration_tests/incremental_configured_catalog.json"
        future_state:
          future_state_path: "integration_tests/abnormal_state.json"
