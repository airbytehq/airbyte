documentationUrl: https://docsurl.com
connectionSpecification:
  $schema: http://json-schema.org/draft-07/schema#
  title: Spacex Api Spec
  type: object
  required:
    - api_key
    - id
  additionalProperties: true
  properties:
    api_key:
      type: string
      description: Dummy as SpaceX doesn't need an API right now, Integrating for future uses
      airbyte_secret: true
    id:
      type: string
      desciption: For a specific ID
