version: 6.6.0

spec:
  type: Spec
  connectionSpecification:
    $schema: http://json-schema.org/draft-07/schema#
    title: Exact Online Spec
    required:
      - access_token
    properties:
      access_token:
        type: string
        description: "Access token for Exact Online API"
        order: 0
        airbyte_secret: true