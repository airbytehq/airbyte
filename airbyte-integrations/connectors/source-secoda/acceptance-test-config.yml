# See [Connector Acceptance Tests](https://docs.airbyte.com/connector-development/testing-connectors/connector-acceptance-tests-reference)
# for more information about how to configure these tests
connector_image: airbyte/source-secoda:dev
acceptance_tests:
  spec:
    tests:
      - spec_path: "manifest.yaml"
  # connection, discovery, basic_read, and full_refresh tests require
  # secrets/config.json which is not available in CI (no GSM secrets configured).
  # To run these locally, create secrets/config.json with a valid api_key.
