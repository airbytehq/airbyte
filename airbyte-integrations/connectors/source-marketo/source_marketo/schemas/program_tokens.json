{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": ["object", "null"],
  "additionalProperties": true,
  "properties": {
    "program_id": {
      "description": "The ID of the parent program from which the tokens were retrieved.",
      "type": ["integer", "null"]
    },
    "name": {
      "description": "The name of the token.",
      "type": ["string", "null"]
    },
    "type": {
      "description": "The data type of the token (e.g., date, number, rich text, score, sfdc campaign, text).",
      "type": ["string", "null"]
    },
    "value": {
      "description": "The value of the token.",
      "type": ["string", "null"]
    },
    "computedUrl": {
      "description": "The computed URL for the token in the Marketo UI.",
      "type": ["string", "null"]
    }
  }
}
