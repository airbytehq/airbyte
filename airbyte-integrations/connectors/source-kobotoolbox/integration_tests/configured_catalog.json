{
    "streams": [
        {
            "stream": {
                "name": "Daily Issue Form",
                "json_schema": {},
                "supported_sync_modes": ["full_refresh", "incremental"],
                "source_defined_cursor": true,
                "default_cursor_field": ["endtime"]
            },
            "sync_mode": "incremental",
            "cursor_field": ["endtime"],
            "destination_sync_mode": "append_dedup"
        }
    ]
}
