`db-harness-lib` contains the engine-independent orchestration for local database connector end-to-end tests. Engine-specific harnesses live in each connector's `.agents/skills/` directory and source these scripts through environment-variable shims; see [README.md](README.md) for the shim contract. This library is for local testing only—never use it against customer connections or Airbyte Cloud.
