// TODO this file should describe what inputs are needed to configure this source (e.g: username and password to sync from a mysql source) using JSONSchema
{
  "documentationUrl": "<URL for documentation goes here, usually something like docs.airbyte.io/integrations/sources/{{dashCase name}}>",
  "connectionSpecification": {
    "$schema": "http://json-schema.org/draft-07/schema#",
    "title": "{{titleCase name}} Spec",
    "type": "object",
    "required": ["fix-me"],
    "additionalProperties": false,
    "properties": {
      "fix-me": {
        "type": "string",
        "description": "describe me"
      }
    }
  }
}
