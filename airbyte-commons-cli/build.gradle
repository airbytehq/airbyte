plugins {
    id "java-library"
}

dependencies {
    implementation 'commons-cli:commons-cli:1.4'
}

Task publishArtifactsTask = getPublishArtifactsTask("commons-cli", "$rootProject.ext.version", project)
