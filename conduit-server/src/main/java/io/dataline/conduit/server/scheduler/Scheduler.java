package io.dataline.conduit.server.scheduler;

public class Scheduler {
    // todo: should this just be a servlet?

    // todo: make the db schema, initialize db on start if it isn't already

    // todo: handle db accessors and such in the commons?

    // todo: provide endpoints that can be called from the server itself?
}
