// import { useDiscoverSchema } from "../useSourceHook";

export const useConnectionService = (/*sourceId: string*/) => {
  //   const { schema, isLoading, schemaErrorStatus, catalogId } = useDiscoverSchema(sourceId);

  //   const createConnection =

  return null;
};
