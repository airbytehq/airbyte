export {};

/**
 * test cases:
 * - should render with correct enabled-at-start styles
 * - should render with correct disabled-at-start styles
 * - should render with correct "added" styles
 * - should render with correct "removed" styles
 * - should render with correct "updated" styles after an update
 * - updating an enabled row should still show added styles
 */
