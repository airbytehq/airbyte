import React from "react";
import { FormattedMessage } from "react-intl";

const UserManagementPage: React.FC = () => {
  return (
    <div>
      <FormattedMessage id="settings.user.management" />
    </div>
  );
};

export default UserManagementPage;
