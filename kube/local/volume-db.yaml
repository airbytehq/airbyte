apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: airbyte-volume-db
  labels:
    airbyte: volume-db
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 500Mi
