apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: airbyte-volume-workspace
  labels:
    airbyte: volume-workspace
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 500Mi
