apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: airbyte-volume-local
  labels:
    airbyte: volume-local
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 500Mi
