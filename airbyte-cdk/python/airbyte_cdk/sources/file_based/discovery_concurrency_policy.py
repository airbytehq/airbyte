class DiscoveryConcurrencyPolicy:
    """
    Used during discovery; allows the developer to configure the number of concurrent
    requests to send to the source.
    """

    n_concurrent_requests = 10
