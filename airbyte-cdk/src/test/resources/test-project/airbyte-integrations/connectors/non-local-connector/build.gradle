plugins {
    id 'base'
}

// Simulate the airbyte-bulk-connector plugin
ext.airbyteBulkConnector = [cdk: 'published']

tasks.named('check') {
    doLast {
        println "Non-local connector check task executed"
    }
}
