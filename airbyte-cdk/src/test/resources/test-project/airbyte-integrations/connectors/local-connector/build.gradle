plugins {
    id 'base'
}

// Simulate the airbyte-bulk-connector plugin
ext.airbyteBulkConnector = [cdk: 'local']

tasks.named('check') {
    doLast {
        println "Local connector check task executed"
    }
}
