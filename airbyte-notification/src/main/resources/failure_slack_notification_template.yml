text: |
  Hey!

  Unfortunately, your connection <connector_source>:<connector_source_version> to <connector_destination>:<connector_destination_version> just failed...
  This happened with job started at <started_at> running for <duration> as <failure_reason>.

  You can access its logs here: <log_url>
