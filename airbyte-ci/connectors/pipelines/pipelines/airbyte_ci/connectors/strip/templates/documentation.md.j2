<!-- THIS FILE IS AUTOGENERATED BY THE AIRBYTE-CI STRIP COMMAND -->

# Source {{source_name | title}}

This page contains the setup guide and reference information for the {{ source_name.capitalize() }} source connector.

## Configuration

| Input | Type | Description | Default Value |
|-------|------|-------------|---------------|
{% for property, details in manifest.get('spec').get('connection_specification').get('properties').items() -%}
| `{{ property }}` | `{{ details.type }}` | {{details.get('title')}}. {{ details.get('description', '') | replace('\n', ' ') }} | {% if 'default' in details %}{{ details.default }}{% endif %} |
{% endfor %}

## Description
TODO: { custom description people can make in the builder }

## Streams
| Stream Name | Primary Key | Pagination | Supports Full Sync | Supports Incremental |
|-------------|-------------|------------|---------------------|----------------------|
{% for stream in manifest.get('streams') -%}
| {{ stream.name }} | {{ ", ".join(stream.get('primary_key', [])) }} | {{ stream.get('retriever', {}).get('paginator', {}).get('pagination_strategy', {}).get('type', 'No pagination') }} | ✅ | {% if stream.incremental_sync is defined %} ✅ {% endif %} |
{% endfor %}
